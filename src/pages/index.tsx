import { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <main
      className={`flex min-h-screen flex-col items-center justify-between p-24 `}
    >
      hello world
    </main>
  );
};

export default Home;
